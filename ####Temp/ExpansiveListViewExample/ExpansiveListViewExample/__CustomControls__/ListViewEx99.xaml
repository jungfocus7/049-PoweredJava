<ListView
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:lc="clr-namespace:ExpansiveListViewExample"
    x:Class="ExpansiveListViewExample.ListViewEx99"
    Padding="0,0,0,0" SelectionMode="Multiple"
    VirtualizingPanel.VirtualizationMode="Recycling"
    BorderBrush="Purple" BorderThickness="2"
    RenderOptions.BitmapScalingMode="NearestNeighbor"
    ScrollViewer.CanContentScroll="True"
    VirtualizingPanel.ScrollUnit="Pixel">
    <ListView.ItemsPanel>
        <ItemsPanelTemplate>
            <lc:VirtualizingStackPanelEx99 IsItemsHost="True" />
        </ItemsPanelTemplate>
    </ListView.ItemsPanel>
    <ListView.View>
        <GridView>
            <GridView.ColumnHeaderContainerStyle>
                <Style TargetType="GridViewColumnHeader">
                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                    <Setter Property="VerticalContentAlignment" Value="Top"/>
                </Style>
            </GridView.ColumnHeaderContainerStyle>
            <GridViewColumn Header="+" Width="30">
                <GridViewColumn.CellTemplate>
                    <DataTemplate>
                        <CheckBox HorizontalAlignment="Left" VerticalAlignment="Top"
                            Margin="0,2,0,0" Background="Transparent"
                            IsHitTestVisible="False" Focusable="False" x:Name="_ccb"/>
                    </DataTemplate>
                </GridViewColumn.CellTemplate>
            </GridViewColumn>
            <GridViewColumn Header="Num" Width="60" DisplayMemberBinding="{Binding Num}"/>
            <GridViewColumn Header="Name" Width="80" DisplayMemberBinding="{Binding Name}"/>
            <GridViewColumn Header="Age" Width="40" DisplayMemberBinding="{Binding Age}"/>
            <GridViewColumn Header="Job" Width="100" DisplayMemberBinding="{Binding Job}"/>
            <GridViewColumn Header="Address" Width="300" DisplayMemberBinding="{Binding Address}"/>
        </GridView>
    </ListView.View>
</ListView>
